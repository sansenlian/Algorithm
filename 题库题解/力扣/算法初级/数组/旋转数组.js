/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function(s, t) {
    let len_t = t.length;
    let len_s = s.length;
    if(len_s!=len_t)return false;
    let _map = new Map();
    
};
let arr = ["h", "e", "l", "l", "o"];
console.log(firstUniqChar("loveleetcode"))

function f() {
    let len = nums.length;
    for (let i = 0; i < len; i++) {

    }
}